/**
 * Local type definitions
 * Re-exports from @picto/types for better DTS generation
 */

export type {
  IconMetadata,
  Plugin,
  SourcePlugin,
  ProcessorPlugin,
  GeneratorPlugin,
} from '@picto/types'
