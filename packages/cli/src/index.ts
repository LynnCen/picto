/**
 * Picto CLI
 *
 * Command-line interface for Picto icon management tool.
 */

import { version } from '@picto/core'

export function cli(): void {
  console.log(`Picto CLI v${version}`)
  console.log('CLI implementation will be added in Phase 1 Sprint 1-2')
}

export { version }
